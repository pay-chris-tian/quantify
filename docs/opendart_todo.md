# OpenDART Integration Todo

## 문서화 원칙 (기본 규칙)

Quantify의 모든 작업은 아래를 기본으로 한다.

1. 작업 시작 전에 목표/범위를 문서에 기록한다.
2. 작업 중에는 진행상황(완료/진행중/이슈)을 즉시 업데이트한다.
3. 작업 종료 후 결과, 남은 이슈, 다음 작업을 반드시 기록한다.
4. 이후 작업도 같은 문서(또는 연결 문서)에서 연속 추적한다.

---

## 현재 상태 (2026-04-18)

- `quantify/dart_loader.py` 초안 구현 완료
- `docs/data_collection.md`에 OpenDART 실행 방법 반영 완료
- 실데이터 수집은 API 키 입력 후 검증 필요

---

## Todo Checklist

## Phase 1. 연결/보안

- [x] `OPEN_DART_API_KEY`를 로컬 환경변수로 설정
- [x] `.env` 또는 OS 환경변수 사용 가이드 확정 (Git 커밋 금지 규칙 포함)
- [x] 인증키 누출 점검 규칙 추가 (`git diff`, `git status` 확인 절차)

## Phase 2. 수집 실행 검증

- [x] 샘플 종목(예: `005930`, `000660`)으로 연간 재무 수집 실행
- [x] `data/universe/dart_corp_codes.csv` 생성 검증
- [x] `data/raw/opendart/<ticker>_financials.csv` 생성 검증
- [ ] API 오류 코드별 동작 점검 (`status`, `message` 로그 확인)

## Phase 3. 데이터 품질/정합성

- [x] 숫자 컬럼 타입 정규화 (문자열 금액 -> numeric)
- [ ] 동일 종목/동일 연도 중복 레코드 처리 규칙 확정
- [ ] 보고서 유형(`annual`, `half`, `q1`, `q3`)별 데이터 일관성 점검
- [ ] 결측치 처리 정책 수립 (N/A, 0, 제외 기준)

## Phase 4. 팩터 계산용 정제 레이어

- [x] `data/processed/`에 표준 재무 테이블 생성 스크립트 추가
- [ ] Value 지표 계산 입력(매출, 영업이익, 자본총계 등) 매핑
- [x] Quality 지표 계산 입력(ROE, 부채비율 등) 매핑
- [ ] 시점 일치 규칙(공시일 기준, 룩어헤드 방지) 문서화

## Phase 5. 운영/자동화

- [ ] 수집 스케줄(예: 주간/월간) 정의
- [ ] 재실행 시 증분 갱신 정책 수립
- [ ] 실패 재시도 및 알림 방식 정의
- [ ] 실행 로그 저장 위치와 보존 정책 확정

## Phase 6. 검증/리포트

- [ ] 수집 성공률 리포트(종목 수, 실패 수, 실패 사유) 자동 생성
- [ ] 샘플 종목 수동 검증(공시 원문 대비 주요 계정값 비교)
- [ ] 백테스트 입력용 최소 컬럼 셋 확정

## Phase 7. Slack 연동 및 의사결정 활용

- [x] Slack Incoming Webhook 생성 및 로컬 환경변수(`SLACK_WEBHOOK_URL`) 설정
- [x] 수집 작업 시작/완료/실패 이벤트를 Slack으로 전송
- [ ] 일일/주간 요약 메시지 템플릿 정의 (수집건수, 실패건수, 주요 이슈)
- [ ] 매수/매도 의견 초안 메시지 포맷 정의
- [ ] 의견결정 로그(근거 지표, 최종 판단, 보류 사유)를 문서와 Slack에 동시 기록
- [ ] 민감정보(인증키/개인정보) 마스킹 규칙을 Slack 전송 전 검증

---

## 작업 기록 (Work Log)

아래 형식으로 계속 추가한다.

```text
YYYY-MM-DD
- 목표:
- 진행:
- 이슈:
- 결정:
- 다음 작업:
```

### 2026-04-18

- 목표: OpenDART 연결 기반 코드 준비
- 진행:
  - `quantify/dart_loader.py` 추가
  - OpenDART 실행 문서 추가/보완
- 이슈:
  - API 키 미설정으로 실데이터 호출 미검증
- 결정:
  - 다음 단계에서 샘플 종목 기준으로 먼저 연결 검증
- 다음 작업:
  - `OPEN_DART_API_KEY` 설정 후 `--tickers` 샘플 실행

### 2026-04-18 (추가)

- 목표: Slack 연동 기반 의사결정 보조 체계 추가
- 진행:
  - OpenDART todo에 Slack 연동 Phase 추가
  - Slack 연동 준비 코드/문서 작업 착수
- 이슈:
  - Slack Webhook URL 미설정
- 결정:
  - 웹훅 기반 최소 연동부터 적용 후, 이후 요약/의견 템플릿 확장
- 다음 작업:
  - `SLACK_WEBHOOK_URL` 설정 후 테스트 메시지 전송

### 2026-04-18 (추가 2)

- 목표: OpenDART + Slack 자동 알림 + 정제 레이어 완료
- 진행:
  - OpenDART 샘플 수집 실검증 완료 (`005930`, `000660`, 2024 연간)
  - `quantify.dart_loader`에 시작/완료/실패 Slack 이벤트 연결
  - `quantify.financials_processor` 추가 (연도별 재무 정제 및 `debt_ratio`, `roe` 산출)
- 이슈:
  - 로컬 환경에서 일부 네트워크 요청은 권한 상승 필요
- 결정:
  - 운영 시에도 실패 이벤트는 Slack으로 즉시 통지
- 다음 작업:
  - 의견결정 메시지 템플릿(매수/매도/보류) 자동 생성 로직 추가
